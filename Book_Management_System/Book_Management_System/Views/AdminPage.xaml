<Page
    x:Class="Book_Management_System.Views.AdminPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Book_Management_System.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="using:Book_Management_System.Models"
    mc:Ignorable="d">

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:Name="Add" Icon="Add" Label="Add" Click="Add_Click" />
            <AppBarButton x:Name="ManageRequest" Icon="Bookmarks" Label="ManageRequest" Click="ManageRequest_Click" />
            <AppBarButton x:Name="findhistory" Icon="Send" Label="History" Click="findhistory_Click" />
            <AppBarButton x:Name="logout" Icon="Stop" Label="LogOut" Click="logout_Click" />
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="VisualStateGroup">
                <VisualState x:Name="VisualStateMin0">
                    <VisualState.Setters>
                        <Setter Target="DetailsView.(UIElement.Visibility)" Value="Collapsed"/>
                        <Setter Target="TodosView.(Grid.ColumnSpan)" Value="2"/>
                    </VisualState.Setters>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1"/>
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="VisualStateMin800">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="800"/>
                    </VisualState.StateTriggers>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <RelativePanel Grid.Row="0">
            <TextBlock x:Name="Title" Style="{StaticResource HeaderTextBlockStyle}">Book Manage System for Admin</TextBlock>
            <SearchBox x:Name="Search" Width="200" Height="40" RelativePanel.AlignRightWithPanel="True" Margin="0,18,10,0" QuerySubmitted="SearchBox_QuerySubmitted" />
        </RelativePanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="0" />
            </Grid.ColumnDefinitions>

            <ScrollViewer x:Name="TodosView" Grid.Row="1"  Margin="20">
                <ListView
                      ItemsSource="{x:Bind ViewModel.Allbooks}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="md:Book">
                            <UserControl>
                                <Grid Height="100">
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup x:Name="state1">
                                            <VisualState>
                                                <VisualState.Setters>
                                                    <Setter Target="image.(UIElement.Visibility)" Value="Collapsed"/>
                                                </VisualState.Setters>
                                                <VisualState.StateTriggers>
                                                    <AdaptiveTrigger MinWindowWidth="1" />
                                                </VisualState.StateTriggers>
                                            </VisualState>
                                            <VisualState>
                                                <VisualState.StateTriggers>
                                                    <AdaptiveTrigger MinWindowWidth="550" />
                                                </VisualState.StateTriggers>
                                            </VisualState>
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>

                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="42"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="100"/>
                                    </Grid.ColumnDefinitions>

                                    <Image x:Name="image" Grid.Column="1" Source="Assets/background.jpg" Height="90" Width="90" Margin="0,3,12,7"/>
                                    <TextBlock Grid.Column="2" Text="{Binding Path=title}" VerticalAlignment="Center" Foreground="Black" FontWeight="Normal" FontSize="15" LineHeight="20" TextWrapping="Wrap" />

                                    <AppBarButton Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Right" IsCompact="True" Icon="Setting" Margin="15,15,0,0">
                                        <AppBarButton.Flyout>
                                            <MenuFlyout>
                                                <MenuFlyoutItem Text="Edit" Click="Edit_Click"/>
                                                <MenuFlyoutItem Text="Share" Click="ShareButton_Click"/>
                                                <MenuFlyoutItem Text="Delete" Click="Delete_Click"/>
                                            </MenuFlyout>
                                        </AppBarButton.Flyout>
                                    </AppBarButton>
                                </Grid>
                            </UserControl>

                        </DataTemplate>
                    </ListView.ItemTemplate>

                </ListView>
            </ScrollViewer>

            <Grid x:Name="DetailsView" Grid.Column="1">
                <Grid>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto">
                        <StackPanel>
                            <Image x:Name="Pic" Source="Assets/background.jpg" Width="350" Height="200" />
                            <RelativePanel Width="350">
                                <AppBarButton x:Name="Select" Icon="Pictures" Label="select" RelativePanel.AlignRightWithPanel="True" RenderTransformOrigin="-2.065,0.628" />
                            </RelativePanel>

                            <TextBox x:Name="_Title"  Width="350" Header="Title" />
                            <TextBox x:Name="Details" Width="350" Height="111" Header="Details" />
                            <DatePicker x:Name="date" Width="350" Header="Due Date" HorizontalAlignment="Center" />
                            <TextBox x:Name="Booknumber" Width="350" Header="Book Number"></TextBox>

                            <RelativePanel Width="350" Margin="0,20,0,0">
                                <Button x:Name="KeyButton" Content="Create" Click="KeyButton_Click"/>
                                <Button x:Name="CancelButton" Content="Cancel" RelativePanel.AlignRightWithPanel="True" Click="CancelButton_Click" />
                            </RelativePanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>

        </Grid>
    </Grid>
</Page>
